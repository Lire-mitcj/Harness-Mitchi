# CursorLoop v2.2 增量实施方案

## 1. 总目标

先跑通最小确定性闭环，再逐步增加辅助能力：

```text
User
  -> Retrieval
  -> Decision LLM
  -> Patch
  -> Executor
  -> Validator
  -> State Update
  -> Loop
```

最终形态：

```text
User
  -> Inter LLM              # hint only
  -> Retrieval              # AST + grep, deterministic
  -> Context Builder        # expansion only
  -> Decision LLM           # single-file edit
  -> Executor               # patch apply
  -> Validator              # test/lint
  -> Harness                # state update
  -> Loop
```

实施原则：

1. 先完成 deterministic runtime，再增加 semantic overlay。
2. Control layer 必须确定性：Retrieval、Executor、Validator、State、Loop。
3. LLM 只存在于语义决策点：Inter 和 Decision。
4. LLM 不持有跨 step 对话记忆。
5. State 是当前快照，不是 memory 或 execution history。
6. 每次 Decision 只能修改一个文件并输出一个 patch。
7. 本方案只改新增 CursorLoop 路径，不处理旧 AgentLoop/Orchestrator。

---

## 2. 仓库路径映射

方案中的逻辑模块映射到当前仓库，不新增平行的顶层包：

| 逻辑路径 | 当前仓库落点 |
|---|---|
| `state/schema.py` | `src/agent/cursor_state.py` |
| `retrieval/core.py` | `src/agent/cursor_retriever.py` |
| `executor/patch.py` | `src/agent/cursor_patch_applier.py`、`src/agent/cursor_executor.py` |
| `validator/run.py` | `src/agent/cursor_validator.py` |
| `llm/decision.py` | `src/agent/cursor_decision.py` |
| `llm/inter.py` | `src/agent/cursor_inter_llm.py` |
| `context/builder.py` | `src/agent/cursor_context_pack_builder.py` |
| `harness/manager.py` | `src/harness/cursor/manager.py` |
| `semantic/tagger.py` | `src/agent/cursor_semantic_tagger.py` |
| `loop.py` | `src/agent/cursor_loop.py` |

---

## 3. Phase 1：最小可运行 Core Loop

### 3.1 目标

Phase 1 的唯一目标：

```text
输入修改请求
  -> 找到候选代码
  -> Decision 生成单文件 patch
  -> 安全应用 patch
  -> 运行 pytest
  -> 更新 State
  -> 成功结束或带错误回环
```

Phase 1 不实现：

- Inter LLM。
- semantic hint/tag/ranking。
- embedding。
- 复杂 scoring system。
- multi-file Decision。
- reasoning carry 或历史 memory。
- Planner、工具编排或动态 workflow。

### 3.2 State Schema

新增：

```text
src/agent/cursor_state.py
```

最小协议：

```python
from dataclasses import dataclass
from typing import Literal


@dataclass(frozen=True, slots=True)
class CursorState:
    task: str
    current_file: str
    last_patch: str
    last_observation: str
    status: Literal["running", "success", "failed"]
```

初始化：

```python
CursorState(
    task=user_input,
    current_file="",
    last_patch="",
    last_observation="",
    status="running",
)
```

约束：

- 无 semantic 字段。
- 无 messages/history 数组。
- 无 plan、step、retry、next 字段。
- `last_patch` 只保存最近 patch，覆盖更新。
- `last_observation` 只保存最近结果，覆盖更新。
- State 序列化后必须控制在 1KB 到 2KB 内。
- 超长 patch/observation 由 Harness 截断，不追加历史。

### 3.3 Retrieval Layer

收敛现有：

```text
src/agent/cursor_retriever.py
```

Phase 1 接口：

```python
async def retrieve(query: str) -> RetrievalResult:
    return {
        "files": grep_files(query),
        "symbols": ast_lookup(query),
    }
```

实际 DTO：

```json
{
  "files": ["src/skills/validator.py"],
  "symbols": [
    {
      "file": "src/skills/validator.py",
      "name": "ValidatorSkill.run",
      "start_line": 208,
      "end_line": 320
    }
  ]
}
```

Phase 1 规则：

1. 只使用 exact path、grep 和 AST/repo symbol index。
2. 固定 provider 集合和固定结果上限。
3. 不使用 embedding。
4. 不使用 semantic hint。
5. 不做 LLM ranking。
6. 不决定最终编辑文件。
7. 相同索引和 query 应产生稳定结果顺序。

### 3.4 Patch Executor

收敛现有：

```text
src/agent/cursor_patch_applier.py
```

可增加薄封装：

```text
src/agent/cursor_executor.py
```

接口：

```python
def apply_patch(file_path: str, patch: str) -> ExecutionResult:
    ...
```

输入只允许 SEARCH/REPLACE：

```text
[7 个 <] SEARCH
exact old code
[7 个 =]
replacement code
[7 个 >] REPLACE
```

实际协议中的三个 marker 分别是连续 7 个 `<`、`=`、`>`；这里拆写是为了避免 Markdown 文件被 Git 识别为未解决冲突。

必须支持：

1. exact match。
2. whitespace-tolerant line match。
3. SEARCH 不存在时 safe fail。
4. 多个 block 先全部 dry-run，再统一写入。
5. 任一 block 失败时文件完全不变。
6. 路径必须位于 project root。
7. 只能修改 Decision 指定的单个文件。
8. 写入使用临时文件替换或等价的原子策略。

返回：

```python
@dataclass(frozen=True, slots=True)
class ExecutionResult:
    success: bool
    file: str
    error: str
```

Executor 不理解任务，不修改 patch，不选择其他文件。

### 3.5 Validator

收敛现有：

```text
src/agent/cursor_validator.py
```

Phase 1 先只运行固定项目测试命令：

```python
async def validate() -> ValidationResult:
    return await run_shell(["pytest"])
```

输出：

```python
@dataclass(frozen=True, slots=True)
class ValidationResult:
    success: bool
    error: str
```

约束：

- 不生成 root cause。
- 不生成修复建议。
- 不根据 semantic hint 选择命令。
- stdout/stderr 截断到固定字符上限。
- timeout、command missing、non-zero exit 都投影成失败 observation。

Phase 1 先保证 `pytest` 跑通；按目标文件选择相关测试、lint 和 syntax check 留到后续小步优化。

### 3.6 Decision LLM

新增：

```text
src/agent/cursor_decision.py
```

严格输出：

```json
{
  "action": "edit | answer | ask_clarify",
  "answer": "",
  "clarification": "",
  "target_file": "src/skills/validator.py",
  "patch": "<<<<<<< SEARCH\n...\n=======\n...\n>>>>>>> REPLACE"
}
```

限制：

1. `edit` 只能有一个 `target_file` 和一个 patch 字符串。
2. target 必须来自 Retrieval 结果。
3. 不允许 files/patches 数组。
4. 不允许 plan、steps、tools、shell、retry、next、done。
5. 不允许 `noop`。
6. Decision 调用始终 `tools=None`。
7. 每轮只接收当前 State 和当前 Retrieval/Context，不回放历史 messages。

Phase 1 不要求输出 reasoning。先保证 schema 稳定和 patch 可执行。

### 3.7 Loop Controller

重写：

```text
src/agent/cursor_loop.py
```

最小循环：

```python
async def run(self, user_input: str):
    state = init_state(user_input)
    retrieval = await self.retriever.retrieve(state.task)
    for _ in range(self.settings.cursor_max_steps):
        decision = await self.decision.decide(
            task=state.task,
            state=state,
            retrieval=retrieval,
        )

        if decision.action == "ask_clarify":
            return decision.clarification

        if decision.action == "answer":
            state = mark_success(state)
            return decision.answer

        execution = self.executor.apply_patch(
            decision.target_file,
            decision.patch,
        )
        state = update_after_execution(state, decision, execution)

        if not execution.success:
            continue

        validation = await self.validator.validate()
        state = update_after_validation(state, validation)

        if validation.success:
            state = mark_success(state)
            return build_success_answer(state)

    return build_failure_answer(state)
```

Phase 1 只支持一次已验证编辑后结束。多文件逐步协调和 validation pass 后再次 Decision，不属于最小闭环，待核心稳定后再增加。

终止条件：

- `ask_clarify`：直接结束。
- `answer`：直接结束；Phase 1 不根据 intent 增加语义完成门禁。
- patch apply fail：更新 observation 后回 Decision。
- validation fail：更新 observation 后回 Decision。
- validation success：State 标记 success 并结束。
- 达到 `cursor_max_steps`：State 标记 failed 并结束。

Decision 对修改请求错误返回 `answer` 属于模型质量问题，不在 Phase 1 通过规则分类补救。Phase 2 接入 Inter 后，它仍只作为 hint；是否增加任务完成评分应作为独立后续能力，不混入最小闭环。

### 3.8 Phase 1 内部交付顺序

按照依赖顺序实施，不一次性铺开全部抽象：

1. `cursor_state.py`：State schema、初始化和大小测试。
2. `cursor_patch_applier.py`：exact、whitespace tolerant、safe fail、atomic apply。
3. `cursor_loop.py`：先注入 fake Decision/fake Validator，跑通 skeleton。
4. `cursor_decision.py`：接真实 LLM 和严格 JSON parser。
5. `cursor_validator.py`：接固定 `pytest`。
6. 整体集成测试：真实临时文件修改、测试失败回环、测试成功结束。

### 3.9 Phase 1 验收标准

- 能从 grep/AST 候选中让 Decision 选择一个文件。
- 能应用单文件 SEARCH/REPLACE patch。
- patch mismatch 不产生部分写入。
- 能执行 `pytest` 并捕获错误。
- validation fail 后 Decision 能基于最新 observation 重试。
- validation success 后 State 为 `success`。
- State 不随循环次数增长。
- Prompt 不包含历史 assistant messages。

---

## 4. Phase 2：Inter LLM

### 4.1 目标

Inter 只增加可观测 hint，不改变 Phase 1 的控制行为。

新增：

```text
src/agent/cursor_inter_llm.py
```

输出：

```json
{
  "intent": "modify | explain | debug",
  "confidence": 0.7
}
```

规则：

- 不影响 Retrieval provider、query 或数量上限。
- 不影响 Decision schema。
- 不影响 Executor、Validator、State 和 Loop 分支。
- 只用于日志、状态事件和可选 prompt hint。
- JSON 无效时 `hint=None`，不运行规则分类器。
- 每个用户 turn 最多调用一次。

接入后数据流：

```text
User -> Inter hint
     -> 原有固定 Retrieval
     -> 原有 Decision/Execute/Validate loop
```

Phase 2 验收：相同用户输入在有/无 Inter 情况下，RetrievalConfig 和硬门禁完全一致。

---

## 5. Phase 3：Context Pack Builder

### 5.1 目标

把原始 Retrieval 结果展开成更适合 Decision 阅读的代码窗口，但不做筛选。

修改：

```text
src/agent/cursor_context_pack_builder.py
```

接口：

```python
def build_context(result: RetrievalResult) -> ContextPack:
    return ContextPack(
        windows=get_code_windows(result.files, result.symbols),
        symbols=result.symbols,
    )
```

规则：

- 只展开 Retrieval 已返回的文件和 symbol。
- 不新增候选。
- 不删除候选。
- 不按语义重新排序。
- 不选择最终目标。
- 单文件窗口和总 ContextPack 均有固定大小上限。
- ContextPack 是当轮临时值，不写入 State。

接入后 Decision 输入从 `retrieval` 替换为 `context_pack`，其余闭环不变。

---

## 6. Phase 4：Harness State Manager

### 6.1 目标

将散落在 Loop 中的 State 更新、压缩和格式化集中到 Harness。

新增：

```text
src/harness/cursor/__init__.py
src/harness/cursor/manager.py
```

核心接口：

```python
class CursorStateManager:
    def initial(self, task: str) -> CursorState:
        ...

    def after_execution(
        self,
        state: CursorState,
        target_file: str,
        patch: str,
        result: ExecutionResult,
    ) -> CursorState:
        ...

    def after_validation(
        self,
        state: CursorState,
        result: ValidationResult,
    ) -> CursorState:
        ...

    def format_for_prompt(self, state: CursorState) -> str:
        ...
```

更新语义：

```python
def after_validation(state, observation):
    return CursorState(
        task=state.task,
        current_file=state.current_file,
        last_patch=bounded(state.last_patch),
        last_observation=bounded(observation.error),
        status="success" if observation.success else "running",
    )
```

Harness 必须保证：

- State 序列化后不超过配置上限，默认 2KB。
- 只保留最近 patch 和 observation。
- 不保存 messages、reasoning 或历史结果。
- State 更新使用新对象，不允许 LLM 或 Executor 原地修改。
- Prompt formatting 不附加旧对话。

Phase 4 完成后，`CursorLoop` 不再直接构造或修改 State。

---

## 7. Phase 5：Semantic Layer（可选、最后实施）

### 7.1 前置条件

只有以下条件全部满足后才能开始 Phase 5：

- Phase 1 闭环稳定。
- patch 原子性测试通过。
- validation fail/success 回环测试通过。
- State 大小测试通过。
- Inter 不影响控制流的边界测试通过。

### 7.2 Semantic Tagger

新增：

```text
src/agent/cursor_semantic_tagger.py
```

接口：

```python
def annotate(context_pack: ContextPack) -> SemanticAnnotations:
    ...
```

规则：

- tags 只用于 display 或 Decision prompt 注释。
- 不影响 Retrieval provider、候选集合或结果上限。
- 不影响 scope、target_file 合法性或 patch apply。
- 不影响 Validator 命令和 pass/fail。
- 不进入 CursorState。
- 首版使用确定性 tag 规则，不增加第三个 LLM。

可选 ContextPack 表示：

```json
{
  "file": "src/skills/validator.py",
  "content": "...",
  "semantic_tags": ["validation", "sql_check"]
}
```

Phase 5 不引入 semantic ranking。若未来需要 ranking，必须作为独立变更评审，并证明不会改变硬候选集合和控制边界。

---

## 8. 完整实施顺序

```text
Milestone 1
  State schema
  Patch executor
  Loop skeleton with fakes

Milestone 2
  Decision LLM
  Strict schema parser
  Single-file patch integration

Milestone 3
  Validator pytest integration
  Fail -> retry
  Pass -> success

Milestone 4
  Inter LLM logging hint

Milestone 5
  Context Pack expansion

Milestone 6
  Harness State Manager

Milestone 7
  Optional semantic tags
```

不要在 Phase 1 同时引入 Inter、semantic tags、embedding、reasoning carry 或 multi-file loop。最小闭环未稳定前，任何语义增强都属于阻塞项外工作。

---

## 9. 测试计划

### 9.1 State

1. 初始化字段正确。
2. 更新采用覆盖语义。
3. 多轮更新后 State 大小不增长。
4. 序列化大小不超过 2KB。
5. State 不包含 history/messages/semantic 字段。

### 9.2 Retrieval

6. exact path 命中。
7. grep 命中。
8. AST symbol 命中。
9. 无 embedding 调用。
10. 结果顺序稳定并受固定上限约束。

### 9.3 Executor

11. exact SEARCH/REPLACE 成功。
12. whitespace tolerant match 成功。
13. SEARCH 不存在时 safe fail。
14. 多 block 任一失败时文件完全不变。
15. project root 外路径被拒绝。

### 9.4 Decision

16. 只接受 edit/answer/ask_clarify。
17. edit 只接受单 target_file 和单 patch。
18. plan、tools、files/patches 数组被拒绝。
19. target_file 不在候选中被拒绝。
20. Decision 始终以 `tools=None` 调用。

### 9.5 Validator 与 Loop

21. pytest exit 0 返回 success。
22. pytest non-zero 返回截断后的 error。
23. patch fail 更新 observation 并回 Decision。
24. validation fail 更新 observation 并回 Decision。
25. validation pass 标记 State success 并结束。
26. step 上限耗尽标记 failed。
27. Prompt 不回放历史 assistant messages。

### 9.6 后续阶段边界

28. Inter 开关不改变 Retrieval 和硬门禁。
29. Context Builder 不新增或删除 Retrieval 候选。
30. Harness 保证 State 大小上限。
31. Semantic tags 不改变控制流和 Validator 结果。
32. `/plan` 旧路径不受影响。

---

## 10. Definition of Done

Phase 1 完成必须同时满足：

- 用户请求可以命中候选代码。
- Decision 可以输出合法单文件 patch。
- Executor 可以安全应用 patch。
- Validator 可以运行 `pytest`。
- 失败结果可以进入下一轮 Decision。
- 成功结果可以更新 State 并结束。
- State 始终是固定大小快照。
- 测试覆盖 patch 原子性和完整成功/失败闭环。

最终 v2.2 完成还需要：

- Inter 仅作为 hint，不影响控制。
- Context Builder 只 expansion。
- Harness 独占 State 更新。
- Semantic layer 可完全关闭，关闭后核心闭环行为不变。

---

## 11. 最终职责边界

```text
Inter LLM            optional semantic hint only
Retrieval            deterministic AST + grep
Context Builder      deterministic expansion
Decision LLM         one local decision, one file, one patch
Executor             deterministic patch application
Validator            deterministic test observation
Harness              bounded snapshot update and formatting
CursorLoop           deterministic control flow
Semantic Tagger      optional display annotation only
```

一句话总结：

```text
先做 deterministic runtime，再逐步加 semantic overlay。
```
